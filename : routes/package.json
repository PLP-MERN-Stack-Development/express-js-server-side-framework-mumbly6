{
  "name": "express-restful-api",
  "version": "1.0.0",
  "description": "RESTful API with Express.js for product management",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "express",
    "rest",
    "api",
    "crud",
    "products"
  ],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "body-parser": "^1.20.2",
    "uuid": "^9.0.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
```

---

## 📝 **FILE 9: .env.example**

**CREATE** this file (if you don't have it):
```
# Server Configuration
PORT=3000

# API Security
API_KEY=your_secret_api_key_here

# Environment
NODE_ENV=development
```

---

## 📝 **FILE 10: .gitignore**

**CREATE** this file (if you don't have it):
```
# Dependencies
node_modules/

# Environment variables
.env

# Logs
logs/
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
